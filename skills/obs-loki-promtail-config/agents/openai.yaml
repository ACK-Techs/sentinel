interface:
  display_name: "Loki: Promtail Config"
  short_description: "Promtail scrape config, pipeline stages ve multi-line log bir…"
  default_prompt: "Use $obs-loki-promtail-config to complete this task."

policy:
  allow_implicit_invocation: true
