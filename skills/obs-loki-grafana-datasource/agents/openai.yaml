interface:
  display_name: "Loki: Grafana Datasource"
  short_description: "Grafana'da Loki datasource, derived field ve trace-log korela…"
  default_prompt: "Use $obs-loki-grafana-datasource to complete this task."

policy:
  allow_implicit_invocation: true
