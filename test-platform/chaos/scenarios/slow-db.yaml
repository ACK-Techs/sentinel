name: slow-db
description: Orders database latency spike
duration_s: 600
warmup_s: 60
cooldown_s: 60
load:
  scenario: steady
expected_symptoms:
  - p99_latency_spike
  - db_wait_time_up
steps:
  - at_s: 0
    target: orders
    chaos:
      profile: slow-db

